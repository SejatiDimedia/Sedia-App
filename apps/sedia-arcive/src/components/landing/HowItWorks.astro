---
const steps = [
  {
    number: "01",
    title: "Upload",
    description: "Drag and drop your files or folder. Support for all file types with fast, secure uploads.",
  },
  {
    number: "02",
    title: "Organize",
    description: "Create folders, add tags, and keep your workspace tidy with smart management tools.",
  },
  {
    number: "03",
    title: "Share",
    description: "Collaborate with your team. Set granular permissions and share secure links instantly.",
  },
];
---

<section id="how-it-works" class="py-24 bg-white relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <!-- Header -->
    <div class="text-center max-w-2xl mx-auto mb-16">
      <h2 class="text-3xl sm:text-4xl font-bold text-zinc-900 tracking-tight">
        Workflow made simple
      </h2>
      <p class="mt-4 text-base sm:text-lg text-zinc-600">
        Get started in minutes. No complex setup or steep learning curve.
      </p>
    </div>

    <!-- Steps -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 relative">
      <!-- Connecting Line (Desktop) -->
      <div class="hidden md:block absolute top-12 left-[16%] right-[16%] h-0.5 bg-gradient-to-r from-zinc-200 via-primary-200 to-zinc-200 -z-10"></div>

      {steps.map((step) => (
        <div class="relative flex flex-col items-center text-center">
          <!-- Number Bubble -->
          <div class="w-24 h-24 rounded-full bg-white border-4 border-zinc-50 flex items-center justify-center mb-6 shadow-sm z-10">
            <span class="text-3xl font-black text-transparent bg-clip-text bg-gradient-to-br from-primary-600 to-primary-400">
              {step.number}
            </span>
          </div>

          <h3 class="text-xl font-bold text-zinc-900 mb-2">{step.title}</h3>
          <p class="text-zinc-600 max-w-xs leading-relaxed">
            {step.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>
